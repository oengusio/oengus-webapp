version: '3.8'

services:
  app:
    image: oenguswebapp
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
      DOMAIN: oengus.dev
      API: oengus.dev
      # Only set DOMAIN_V1 if you want redirects
      # DOMAIN_V1: v1.oengus.dev
      # CACHE_HOST: cache
    ports:
      - 3000:3000

  # cache:
  #   image: memcached:1-alpine
